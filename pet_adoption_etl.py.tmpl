import sys
from awsglue.transforms import *
from awsglue.utils import getResolvedOptions
from pyspark.context import SparkContext
from awsglue.context import GlueContext
from awsglue.job import Job

args = getResolvedOptions(sys.argv, ['JOB_NAME'])
sc = SparkContext()
glueContext = GlueContext(sc)
spark = glueContext.spark_session
job = Job(glueContext)
job.init(args['JOB_NAME'], args)

datasource = glueContext.create_dynamic_frame.from_options(
    connection_type="s3",
    connection_options={"paths": ["s3://${bucket}/input/pet_adoptions.csv"]},
    format="csv",
    format_options={"withHeader": True}
)

dogs = Filter.apply(frame=datasource, f=lambda x: x["PetType"] == "Dog")
dogs_renamed = dogs.rename_field("PetID", "ID").rename_field("Name", "PetName")

glueContext.write_dynamic_frame.from_options(
    frame=dogs_renamed,
    connection_type="s3",
    connection_options={"path": "s3://${bucket}/output/"},
    format="parquet"
)

job.commit()